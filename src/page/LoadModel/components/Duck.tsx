import { useGLTF } from '@react-three/drei';

const Fox = (props: any) => {
  //useGLTP内部处理draco解压问题
  // const { scene } = useGLTF('/models/Duck/glTF-Draco/Duck.gltf');
  // return <primitive {...props} object={scene} />;
};
// 预加载模型
// useGLTF.preload('/models/Duck/glTF-Draco/Duck.gltf');
export default Fox;
